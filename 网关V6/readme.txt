V1 实现基本功能
V2 更改接收lora数据帧逻辑，使其能根据接收数据帧中不同的设备码发送不同的节点数据
V3 增加了接收云平台下发的浊度阈值以及ph阈值的功能。解析这两个数据并发送对应数据帧给目标节点
V4 解决lora初始化概率性失败的问题
V5 正确处理了个节点板在云平台显示上下线的问题
V6 功能无改变，在协议的传输上由节点主动发送改为了网关对对应节点发送请求帧后再发送对应数据帧。更加符合lora多节点和网关通信的规范。同时解决多个节点与网关通信时网关的通信压力，但降低了实时性。

如果要体现实时性：用V5版，如果要体现协议的规范性，用V6版。

